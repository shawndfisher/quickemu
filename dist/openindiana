#!/usr/bin/env bash

INFO="OpenIndiana|-|https://www.openindiana.org/|Community supported illumos-based operating system."

GUEST="solaris"

function dist_releases() {
    #shellcheck disable=SC2046,SC2005
    echo $(web_pipe "https://dlc.openindiana.org/isos/hipster/" | grep link | cut -d'/' -f 1 | cut -d '"' -f4 | sort -r | tail -n +2 | head -n 5)
}

function dist_editions() {
    echo gui text minimal
}

function dist_image_get() {
    local HASH=""
    local ISO=""
    local URL=""
    URL="https://dlc.openindiana.org/isos/hipster/${RELEASE}"
    ISO="OI-hipster-${EDITION}-${RELEASE}.iso"
    HASH=$(web_pipe "${URL}/${ISO}.sha256" | cut -d' ' -f1)
    echo "${URL}/${ISO} ${HASH}"
}

function dist_vm_option_tweak() {
    echo "boot=\"legacy\""
    echo "disk_size=\"32G\""
}

# vim:tabstop=4:shiftwidth=4:expandtab
